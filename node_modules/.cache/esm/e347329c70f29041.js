let express,mongoose,cors,readdirSync;_70f‍.w("express",[["default",["express"],function(v){express=v}]]);_70f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_70f‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_70f‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);




const morgan = require("morgan");
require("dotenv").config();

const app = express();
const http = require("http").createServer(app);
const io = require("socket.io")(http, {
  cors: {
    origin: "http://localhost:3000",
    methods: ["GET", "POST", "PUT"],
    allowedHeaders: ["Content-type"],
  },
});

mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
  })
  .then(() => console.log("DB Connected"))
  .catch((err) => _70f‍.g.console.log("Error connecting to DB", err));

//middlewares
app.use(
  express.json({
    limit: "5mb",
  })
);
app.use(express.urlencoded({ extended: true }));
app.use(
  cors({
    origin: [process.env.CLIENT_URL],
  })
);

readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

// io.on("connect", (socket) => {
//   //console.log(`SOCKET.IO`, socket.id));
//   socket.on("send-message", (message) => {
//     // console.log("new message received", message);
//     socket.broadcast.emit("receive-msg", message);
//   });
// });

io.on("connect", (socket) => {
  //console.log(`SOCKET.IO`, socket.id));
  socket.on("new-post", (newPost) => {
    //console.log("new message received", newPost);
    socket.broadcast.emit("new-post", newPost);
  });
});

const port = process.env.PORT || 8000;

http.listen(port, () => console.log(`Server is flying`));
